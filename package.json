{
  "name": "jarvis",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "backend": "cd backend && poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:all": "concurrently -n backend,web -c blue,green \"npm run backend\" \"npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
